import { ProductFilterYearPipe } from './product-filter-year.pipe';

describe('ProductFilterYearPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterYearPipe();
    expect(pipe).toBeTruthy();
  });
});
