import { ProductFilterNamePipe } from './product-filter-name.pipe';

describe('ProductFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
