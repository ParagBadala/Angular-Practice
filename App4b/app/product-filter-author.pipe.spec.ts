import { ProductFilterAuthorPipe } from './product-filter-author.pipe';

describe('ProductFilterAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
